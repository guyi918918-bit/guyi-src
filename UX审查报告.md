# 工作台 UX 体验审查报告

> 审查对象：单文件 HTML 工作台（DSC 销售 · 项目管理工作台）
> 审查方式：基于源码静态走查（`_extract/copy1.html` 5209 行 + `workbench/patch/*` 注入补丁），不依赖口头描述
> 审查视角：10 年经验 UX 专家 / 产品经理
> 审查维度：功能完整性 · 交互体验 · 信息呈现 · 容错与异常 · 一致性 · 可发现性

---

## 一、逐功能审查

### 1. 全局导航与信息架构
- **体验评分**：⭐⭐⭐（3/5）
- **发现的问题**
  - 🟡 中等｜顶层 6 板块（dash/inspire/work/projects/clients/recycle）切换为"整面板显隐"，无过渡动画、无面包屑、无二级标题联动（`copy1.html:5177-5200`）。用户切换时缺少"我在哪"的连续感。
  - 🟡 中等｜配置类入口藏得深：云同步在弹窗、情报"检索设置"默认 `display:none` 需点 `intelCfgToggle` 展开（`dash_panels.html:16`）。低频但重要的功能发现成本高。
  - 🟢 轻微｜板块内子标签（如 work 下 daily/weekly/monthly/annual/stat/history）与 6 大板块并存，信息架构层级未在一处统一表达，新用户容易"迷路"。
- **优化建议**
  - 切换板块加 150–200ms 淡入过渡 + 当前板块标题同步到页面 H1，强化"位置感"。
  - 把"云同步/检索设置"等低频配置收敛到一个「⚙ 设置」统一入口，避免散落。
  - 顶部导航增加板块下"子模块"的可见指示（如 work 板块高亮时，下方露出日报/周报标签条）。
- **可新增功能建议**：全局命令面板（Cmd/Ctrl+K）一键跳转到任意板块、任意操作；降低深层功能发现成本。

### 2. 情报搜集（dash）
- **体验评分**：⭐⭐⭐⭐（4/5）
- **发现的问题**
  - 🟡 中等｜"检索设置"默认收起（`dash_panels.html:16`），用户首次进来可能不知道能配置关键词/省份/来源，导致情报"不全"却无从下手。
  - 🟢 轻微｜时间窗口（7d/30d/90d/180d/1y）与"时间排序"两个下拉并列，但缺少"当前筛选的可见范围"的实时文字摘要，用户不确定自己看的是哪段。
- **优化建议**
  - 首次进入情报板块时，"检索设置"默认展开一次（或顶部常驻一个显眼的「⚙ 配置情报源」按钮）。
  - 在列表上方加一行小字："当前：近 30 天 · 按最新优先 · 6 个关键词"，让用户对筛选状态有掌控感。
- **可新增功能建议**：情报"已读/收藏"状态（目前只有"可借鉴"标识，缺个人化管理）；关键词命中高亮；情报与"客户/项目"双向关联（某条情报一键转成商机线索）。

### 3. 灵感早报（inspire）
- **体验评分**：⭐⭐⭐⭐（4/5）
- **发现的问题**
  - 🟢 轻微｜案例卡（省份/来源/类型/亮点/可学/可复制/链接）信息层级清晰，但"可学/可复制"两个动作与"原文链接"三者并列，主次略模糊，用户可能忽略最有价值的"可复制"动作。
  - 🟢 轻微｜与 dash 共用一套 `dash-btn`/`dash-card` 类名，但交互细节（如批量删除入口位置）与情报略有差异，需统一。
- **优化建议**
  - 把"可复制"做成主按钮（填充色），"可学"做成次级，"原文链接"做成外链图标，拉开主次。
  - 与情报板块统一"批量操作栏"的位置与样式。
- **可新增功能建议**：灵感"一键转日程/待办"（看到好案例直接生成一条执行任务）；灵感按"可借鉴度"打分排序。

### 4. 日常工作（work：日报/周报/月报/年度/历史）
- **体验评分**：⭐⭐⭐（3/5）
- **发现的问题**
  - 🔴 严重｜反馈机制不统一：日报/周报/月报的保存成功、校验失败一律用**原生 `alert()`**（全文约 40 处），阻塞式弹窗、无自动消失、体验割裂（`copy1.html` 无 `toast()` 定义，而 dash/enhance 域有统一 `toast`）。同一产品两套反馈语言。
  - 🔴 严重｜**加载态缺失**：9 个主面板在渲染/拉取/保存时无任何 loading 反馈（`loading|加载|spinner` 零命中），仅 dash/inspire 有"加载中…"文字。日报、项目保存是同步阻塞，大数据量时用户会以为"卡死"。
  - 🟡 中等｜"清空当前表单"仅有 `confirm` 无撤销（`copy1.html:3629`）；日报/周报/月报"该日期已有记录，覆盖？"`confirm` 直接覆盖，无"另存为新版本"选项。
  - 🟢 轻微｜"继承昨日"（`fillYesterday`）是好功能，但入口不够显眼，部分用户可能不知道能一键带出昨天内容。
- **优化建议**
  - **全局统一反馈层**：在 `copy1.html` 引入 `toast()`（复用 enhance.js 已有的实现），把 40 处 `alert` 分批替换为非阻塞 toast；错误类用红色、成功类用绿色、2–3 秒自动消失。
  - **补齐加载态**：保存/渲染时按钮进入 loading（旋转图标 + 禁用），大列表首屏加骨架屏。
  - 日期冲突时不只"覆盖/取消"，增加"另存为新条目"第三选项；清空表单前提示"已自动生成一次本地草稿备份"。
  - "继承昨日"按钮提到表单顶部并加图标，作为主操作。
- **可新增功能建议**：日报/周报模板库；周报/月报自动从日报聚合生成（减少重复录入）；历史记录全文搜索。

### 5. 项目管理（projects）
- **体验评分**：⭐⭐⭐⭐（4/5）
- **发现的问题**
  - 🟢 轻微｜项目卡信息密度高（财务三栏 + 进度条 + 步骤预览 + 起止日期 + 标签 + 操作），单卡略重，长列表滚动时认知负担大。
  - 🟢 轻微｜排序靠 ↑/↓ 按钮（`copy1.html:3068`），无拖拽，批量调整顺序效率低。
- **优化建议**
  - 卡片默认"精简视图"（名称 + 状态 + 金额 + 进度条 + 倒计时），点开再展开财务明细与步骤。
  - 支持拖拽排序或在列表头点击排序（状态/金额/截止日），替代逐个 ↑/↓。
  - 保持金额 💰/✅/📌 三栏的强视觉锚点（当前做得好，保留）。
- **可新增功能建议**：项目"阶段看板"（待启动→进行中→回款中→完成）拖拽流转；项目与关联客户/合同/回款打通成一条时间线；项目逾期/回款预警通知。

### 6. 客户管理（clients）
- **体验评分**：⭐⭐⭐（3/5）
- **发现的问题**
  - 🟡 中等｜**客户卡信息过载**：名称 + 级别/消费/紧密标签 + 联系人 + 电话 + 行业 + 备注 + 过往消费 + 活动预警全部平铺（`copy1.html:2681-2711`），无折叠、无头像/主指标视觉锚点，一屏看不了几个客户。
  - 🟡 中等｜筛选/排序可用（级别/消费/紧密），但**无批量操作除删除外**（不能批量改级别、批量加标签、批量导出选中）。
  - 🟢 轻微｜"活动预警"标红做得好（`copy1.html:2688`），但预警触发规则对普通用户不透明（什么算"紧密"？什么算"预警"？）。
- **优化建议**
  - 客户卡改为"头图（首字母头像/行业色块）+ 名称 + 一级指标（如最近活动/待跟进）+ 展开看详情"的紧凑结构。
  - 增加批量编辑（级别、标签）、批量导出选中、批量发送提醒。
  - 预警旁加 `?` 悬浮说明触发规则，降低焦虑。
- **可新增功能建议**：客户"跟进时间线"（每次联系/活动自动记录）；客户分层自动化（按消费+紧密自动标 A/B/C 级）；客户与项目/情报联动视图。

### 7. 回收站（recycle）
- **体验评分**：⭐⭐⭐⭐（4/5）
- **发现的问题**
  - 🟢 轻微｜`clearRecycleBin` 永久删除有 `confirm` 且文案"永久删除"清晰（`copy1.html:2500`），但回收站容量自动裁剪（保留最近 300 条）对用户**不可见**——用户可能困惑"我删的那条怎么找不到了"。
  - 🟢 轻微｜恢复后回到原板块的路径不够直观（恢复成功仅 toast，未提示"已回到 XX 板块"）。
- **优化建议**
  - 回收站顶部加一行说明："自动保留最近 300 条，更早的已清理释放云空间"，把"隐形规则"显性化。
  - 恢复成功后给出可点击的"查看"链接，直达原位置。
- **可新增功能建议**：回收站按类型（客户/项目/日报/历史）分组的批量恢复；回收站"最近 7 天删除"快捷筛选。

### 8. 云同步（Supabase）
- **体验评分**：⭐⭐⭐⭐（4/5）
- **发现的问题**
  - 🟡 中等｜连接失败的错误映射做得细（`Failed to fetch`→网络不可达、`PGRST125`→表不存在、`401`→Key 无效，`enhance.js:645-652`），但**成功/同步中的状态反馈仍部分走 `alert`**，与 toast 并存。
  - 🟡 中等｜首次配置需填 3 个值（URL/anon key/空间 ID），无"一键从旧设备迁移"引导，换设备或换部署平台（如本次切到 GitHub Pages）时容易卡在"数据没拉回来"。
  - 🟢 轻微｜"整理云空间"按钮有价值，但空间占用面板的百分比计算对普通用户偏技术（字节/占比），缺乏"还能用多久"的直观判断。
- **优化建议**
  - 同步相关反馈统一走 toast + 右上角常驻状态徽标（绿/蓝/红/灰四态已存在，保留并强化）。
  - 配置页增加"迁移指引"：从另一设备导出 JSON 备份 → 此处导入即完成迁移，降低跨设备门槛。
  - 空间面板用"进度环 + 预计可用 X 月"替代纯数字。
- **可新增功能建议**：多设备"最后同步设备/时间"展示；冲突时弹窗让用户"选云端/选本地"而非默认 last-write-wins；同步失败自动重试（指数退避）。

### 9. 数据管理（备份/恢复/导入导出）
- **体验评分**：⭐⭐⭐（3/5）
- **发现的问题**
  - 🔴 严重｜顶部"恢复"按钮与"备份"并列，**覆盖全部数据却无任何防护提示**（仅导入时有 `confirm`，`copy1.html:4896`）。误点"恢复"选错文件即全量覆盖，且**无操作级撤销**（见下条），属于高危路径。
  - 🔴 严重｜**无操作级撤销**：全文无 `undo`/`撤销` 机制，仅"回收站恢复"和"全量备份/恢复"两种粗粒度兜底（`copy1.html` 无 undo）。误编辑/误清空表单无法逐步回退。
  - 🟡 中等｜导入 Excel 报错文案偏技术（`标题行必须包含"客户名称"列`，`copy1.html:2769`），普通用户不易理解。
- **优化建议**
  - "恢复"按钮改为二次确认 + 恢复前自动生成一次本地快照（已备份可回退），并把按钮视觉降级（次级/描边）与"备份"区分。
  - 引入轻量撤销：表单编辑用"草稿态"，保存前可取消；至少对"清空表单/删除单条"提供 5 秒内的撤销 toast（"已删除，撤销"）。
  - 导入报错改成"请在第一行表头包含【客户名称】列，参考模板"并附一键下载模板。
- **可新增功能建议**：自动定时本地快照（每日）+ 快照时间线可回滚；导出时按板块/时间范围选择性导出。

### 10. 全局可用性（PWA / 暗色 / 引导 / 键盘）
- **体验评分**：⭐⭐（2/5）
- **发现的问题**
  - 🔴 严重｜**无新手引导 / 帮助中心**：全量检索 `tour|引导|onboarding|新手|帮助|tooltip` 均无匹配（`copy1.html` 与补丁均无）。新用户面对 6 板块 + 云同步 + 检索设置，无 first-run 引导、无 `?` 帮助入口。
  - 🔴 严重｜**无快捷键 / 无拖拽**：全文唯一键盘绑定是待办输入框 Enter 提交（`dash.js:623`），无 Ctrl+S 保存、"/" 搜索、j/k 导航；排序全靠 ↑/↓ 按钮。
  - 🟡 中等｜视觉语言分裂：work/clients/projects 用 `.btn-*`/`.client-item` 一套；dash/inspire 用 `.dash-btn`/`.dash-card`；enhance 用 `.mod-input`/`.sync-*`（探索报告 7.2）。同一产品三套类名体系并存，`copy1.html` 甚至有内联样式按钮（`copy1.html:1572`）。
  - 🟢 轻微｜暗色模式有（base 已定义 `toogleDarkMode`），PWA 静默更新已做（sw.js），这两项是亮点。
- **优化建议**
  - **短期必做**：加一个首次运行的 3 步引导（① 这是你的 6 大板块 ② 先配置云同步保数据 ③ 从日报开始记录），用轻量 overlay 实现，可跳过。
  - **统一设计令牌**：把三套类名收敛到一套 `.btn-*`/`.card-*`/`.empty-*`/`.toast`，消除内联样式；CSS 变量 `--primary` 在 copy1 与 enhance 间对齐（enhance.js 用了 `var(--primary,#0066cc)` 兜底，说明原版未定义）。
  - **补快捷键**：至少支持 `/` 聚焦搜索、Ctrl+S 保存当前表单、Esc 关闭弹窗、j/k 或 ←/→ 切换板块。
- **可新增功能建议**：命令面板（Cmd+K）；空状态引导式创建（"创建第一条日报"按钮直达）；帮助中心侧栏（FAQ + 短视频）。

---

## 二、总体汇总

### Top 5 最影响体验的问题（按严重程度）
1. 🔴 **反馈机制割裂 + 加载态缺失**（日常工作等板块）：约 40 处原生 `alert` + 9 面板无 loading，用户感知"卡顿/失控"。影响范围最广、最日常。
2. 🔴 **无操作级撤销 + 恢复按钮无防护**（数据管理）：高危覆盖路径无兜底，误操作的代价是全局数据丢失。
3. 🔴 **零新手引导 / 零快捷键**（全局可用性）：新用户上手门槛高，老用户效率天花板低。
4. 🟡 **视觉语言三套并存 + 配置入口深**（一致性/可发现性）：同一产品三种类名体系，云同步/检索设置藏得深，专业感打折。
5. 🟡 **客户卡信息过载 + 云同步跨设备迁移无引导**（客户管理/云同步）：高密度平铺降低浏览效率；换设备/换部署时数据拉不回来易焦虑。

### 最值得新增的 3–5 个功能（按用户价值排序）
1. **首次运行引导 + 帮助中心**（价值最高、成本最低）：直接解决"上手难"，降低你未来每次解释功能的成本。
2. **全局命令面板 Cmd/Ctrl+K**：跳转任意板块/操作、触发搜索，一次投入覆盖所有效率场景。
3. **操作级撤销（5 秒 toast 撤销）**：把"删除/清空"从危险变成可逆，安全感质变。
4. **客户跟进时间线 + 自动分层**：让客户管理从"通讯录"升级为"关系经营"，契合你 ToB 业务本质。
5. **周报/月报从日报自动聚合**：消灭重复录入，日常工作的头号痛点。

### 体验优化路线图
- **短期速赢（1–2 周，低成本高感知）**
  - 统一反馈层：引入 `toast()` 替换主路径 `alert`（先覆盖保存/删除/同步）。
  - 加首次运行 3 步引导 overlay。
  - "恢复"按钮加自动快照 + 视觉降级；"删除/清空"加 5 秒撤销 toast。
  - 补齐日报/项目保存的 loading 按钮态。
- **中期优化（1–2 月，体验质变）**
  - 设计令牌统一：收敛三套类名为一套，消灭内联样式，`--primary` 对齐。
  - 快捷键体系（/、Ctrl+S、Esc、j/k、←/→）。
  - 客户卡紧凑化 + 批量编辑/导出；情报/灵感"配置"入口前移。
  - 云同步"迁移指引" + 空间面板进度环化。
- **长期规划（季度级，产品升级）**
  - 命令面板 Cmd+K；空状态引导式创建；帮助中心。
  - 跨模块联动：情报→商机→项目→客户→回款 一条时间线。
  - 自动化：客户分层、周月报聚合、预警通知、多设备冲突可视化解决。
  - 数据可视化驾驶舱（金额/回款/活动预警的全局视图）。

---

## 三、审查结论
产品**功能闭环完整、危险操作二次确认到位、信息层级（金额/状态/预警）突出、云同步容错较强**——核心能力扎实，说明前期工程投入到位。
主要短板集中在**"人因层"**：反馈不统一、无引导、无撤销、视觉语言分裂、配置入口深。这些问题不致命，但共同拉低了"专业感"和"上手/使用效率"。
建议优先吃下**短期速赢 4 项**（统一 toast、首run 引导、撤销 toast、loading 态），投入小、用户感知最强；中长期再啃设计令牌统一与跨模块联动。

> 注：本报告基于 `2026-08-17` 时点的源码静态走查，未做真机渲染与用户实测。如需对某一板块（如"客户卡完整渲染链""云同步状态机"）做更细的调用图级深挖，可指定。
